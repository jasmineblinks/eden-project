import './assets/main.css'

import { createApp } from 'vue'
// import App from './App.vue'
// import DogInfo from './components/DofInfo.vue'

import router from './router'

// const app = createApp(App)

const routes = [
  //   { path: '/', component: App },
  //   { path: '/dog-info', component: DogInfo, props: true }
]

// app.use(router)
// app.component('DogInfo', DogInfo)
// app.mount('#app')
